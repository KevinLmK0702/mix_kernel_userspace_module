id=fps_boost_ctl
name=fps_boost 帧感知调度控制
version=v2.1.1
versionCode=29
author=Local
description=Material 3 风格 WebUI + 守护进程：按内核跟踪的渲染进程 pid 识别应用（不再每秒 dumpsys）、配置拆分 conf/、热重载与自动合并、黑名单/仅列表内应用；模式预设/帧率档位/RTG/cpufreq 上限感知；新增 WALT governor tunables（auto_boost/pl/hispeed/boost 等，governor 被重选后自动保持）与 rtg_pid 自动兜底；修复手机端卡片右侧被裁（main 缺 border-box）、宽屏导航栏错位、状态卡包名永远显示"无前台渲染进程"。
